/**
 * Optional team defaults. Leave empty to configure in the app Settings.
 * You can also create cloud-config.local.js (gitignored) to override without using the UI.
 */
export const CLOUD_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  webhookUrl: '',
};
